<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 200 200" width="200" height="200" preserveAspectRatio="none" role="img" aria-label="Paper texture placeholder">
  <!-- PLACEHOLDER: replace with the final paper texture (fibre grain, fold shading, etc).
       It is tinted per category by the UI (see components/folded-paper.tsx), so keep this
       file neutral greyscale and low contrast. -->
  <defs>
    <radialGradient id="shade" cx="50%" cy="42%" r="72%">
      <stop offset="0%" stop-color="#ffffff" stop-opacity="0.22" />
      <stop offset="100%" stop-color="#000000" stop-opacity="0.07" />
    </radialGradient>
  </defs>
  <rect width="200" height="200" fill="url(#shade)" />
</svg>
